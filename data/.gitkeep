# This folder holds the SQLite database file (data/notes.db), which is created
# automatically the first time the application runs. The folder itself is kept
# in version control so the database has somewhere to live.
